# GitHub Configuration
GITHUB_USERNAME=your_github_username

# Optional: GitHub Token (only needed for auto-commit mode)
# Leave empty to run in local-only mode (just generates markdown file)
# Add token to enable auto-commit to your repo
GITHUB_TOKEN=

# Optional: Supermemory Configuration
SUPERMEMORY_API_KEY=
SUPERMEMORY_API_URL=https://api.supermemory.ai

# MODES:
# 1. Local Mode (no token): Just generates starred-repos.md locally
# 2. Auto-Commit Mode (with token): Also commits to your GitHub repo
